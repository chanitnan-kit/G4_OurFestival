Configuration (placeholders)

- Database DSN/host/port/name/charset
- Database user/password (load from env, not hardcoded)
- App environment (development/production)
- CORS policy (allowed origins/methods)

Files to add later
- database.php (reads env, returns DSN/config)
- app.php (general app settings)