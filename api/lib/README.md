Library (shared utilities) — placeholders

- db.php: create and expose PDO connection (MySQL)
- session.php: initialize and configure PHP session
- auth.php: current_user(), require_login(), require_role('admin')
- validation.php: simple input validation helpers
- response.php: JSON response helpers (status, payload, error format)

